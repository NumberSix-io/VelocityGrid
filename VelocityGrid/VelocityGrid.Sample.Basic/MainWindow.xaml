<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VelocityGrid.Sample.Basic.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VelocityGrid.Sample.Basic"
    xmlns:velocity="using:VelocityGrid.Managed"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VelocityGrid.Sample.Basic">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,Auto,*" Padding="20" RowSpacing="12">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock Text="VelocityGrid" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="10,000,000 synthetic rows" VerticalAlignment="Center" Opacity="0.7" />
        </StackPanel>
        <Grid Grid.Row="1" ColumnDefinitions="Auto,*" ColumnSpacing="12">
            <StackPanel Orientation="Horizontal" Spacing="8">
                <Button x:Name="RunBenchmarksButton" Content="Run Phase 6 benchmarks" Click="RunBenchmarks_Click" />
                <Button x:Name="StartMarketButton" Content="Start market stream" Click="StartMarket_Click" />
                <Button x:Name="StopMarketButton" Content="Stop" Click="StopMarket_Click" IsEnabled="False" />
            </StackPanel>
            <TextBlock Grid.Column="1" x:Name="BenchmarkStatus" Text="Ready" TextWrapping="Wrap" FontFamily="Consolas" />
        </Grid>
        <Border Grid.Row="2" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
            <velocity:VelocityGridControl x:Name="GridControl" />
        </Border>
    </Grid>
</Window>
