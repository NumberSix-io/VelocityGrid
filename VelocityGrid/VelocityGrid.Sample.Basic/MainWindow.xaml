<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="VelocityGrid.Sample.Basic.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:VelocityGrid.Sample.Basic"
    xmlns:velocity="using:VelocityGrid.Managed"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="VelocityGrid.Sample.Basic">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,*" Padding="20" RowSpacing="12">
        <StackPanel Orientation="Horizontal" Spacing="12">
            <TextBlock Text="VelocityGrid" Style="{StaticResource TitleTextBlockStyle}" />
            <TextBlock Text="1,000 synthetic rows" VerticalAlignment="Center" Opacity="0.7" />
        </StackPanel>
        <Border Grid.Row="1" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" CornerRadius="6">
            <velocity:VelocityGridControl x:Name="GridControl" />
        </Border>
    </Grid>
</Window>
